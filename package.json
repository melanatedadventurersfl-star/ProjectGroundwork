{
  "name": "project-groundwork",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/member-web run build",
    "start": "npm --prefix apps/member-web run start",
    "typecheck": "npm --prefix apps/member-web run typecheck",
    "postinstall": "npm --prefix apps/member-web install"
  },
  "engines": {
    "node": ">=22"
  }
}
