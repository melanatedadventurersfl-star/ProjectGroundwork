name: EAS Android Preview

on:
  push:
    branches:
      - main
    paths:
      - 'apps/mobile/app.json'
      - 'apps/mobile/eas.json'
      - 'apps/mobile/package.json'
      - 'package-lock.json'
      - 'apps/mobile/android/**'
      - 'apps/mobile/ios/**'
      - 'apps/mobile/plugins/**'
      - 'apps/mobile/assets/ma-app-icon.png'
      - 'apps/mobile/assets/ma-splash-icon.png'
      - 'apps/mobile/assets/ma-pathfinder-mark.png'
      - 'apps/mobile/assets/trailhead/**'
      - '.github/workflows/eas-preview-android.yml'
  workflow_dispatch:

# A newer native/runtime build supersedes an older native/runtime build. Unrelated
# commits must not invalidate a build that is already in flight.
concurrency:
  group: eas-preview-android-main
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out triggering native commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Verify build target
        run: |
          CHECKED_OUT="$(git rev-parse HEAD)"
          echo "event_sha=${GITHUB_SHA}"
          echo "checked_out=${CHECKED_OUT}"
          test "${CHECKED_OUT}" = "${GITHUB_SHA}" || {
            echo "Checked-out commit does not match the workflow event SHA."
            exit 1
          }

      - name: Debounce rapid native updates before expensive build
        run: sleep 25

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Canonicalize native Android PNG assets
        run: |
          python3 -m pip install --quiet pillow
          python3 - <<'PY'
          from pathlib import Path
          from PIL import Image, ImageFile

          ImageFile.LOAD_TRUNCATED_IMAGES = True
          paths = [
              Path('apps/mobile/assets/ma-app-icon.png'),
              Path('apps/mobile/assets/ma-splash-icon.png'),
              Path('apps/mobile/assets/ma-pathfinder-mark.png'),
          ]

          for path in paths:
              temp = path.with_name(path.stem + '.canonical.png')
              with Image.open(path) as image:
                  image.load()
                  image.convert('RGBA').save(
                      temp,
                      format='PNG',
                      optimize=False,
                      compress_level=6,
                  )
              temp.replace(path)
              print(f'Canonicalized {path}')
          PY

      - name: Strictly verify PNGs before EAS upload
        run: |
          python3 - <<'PY'
          from PIL import Image

          for path in [
              'apps/mobile/assets/ma-app-icon.png',
              'apps/mobile/assets/ma-splash-icon.png',
              'apps/mobile/assets/ma-pathfinder-mark.png',
          ]:
              with Image.open(path) as image:
                  if image.format != 'PNG':
                      raise RuntimeError(f'{path} is not a PNG.')
                  image.verify()
              with Image.open(path) as image:
                  image.load()
                  print(f'Valid PNG: {path} ({image.width}x{image.height})')
          PY

      - name: Strictly verify Trailhead image assets before EAS upload
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from PIL import Image

          root = Path('apps/mobile/assets/trailhead')
          images = sorted([*root.rglob('*.jpg'), *root.rglob('*.jpeg'), *root.rglob('*.png')])
          if not images:
              raise RuntimeError('No native Trailhead image assets were found.')

          for path in images:
              with Image.open(path) as image:
                  image.verify()
              with Image.open(path) as image:
                  image.load()
                  if image.width < 480 or image.height < 180:
                      raise RuntimeError(f'Trailhead asset is too small: {path} ({image.width}x{image.height})')
                  print(f'Valid Trailhead asset: {path} ({image.width}x{image.height}, {image.format})')
          PY

      - name: Stamp preview APK with triggering commit
        working-directory: apps/mobile
        run: |
          BUILD_GIT_SHA="$(git rev-parse HEAD)"
          export BUILD_GIT_SHA
          node <<'NODE'
          const fs = require('fs');
          const eas = JSON.parse(fs.readFileSync('eas.json', 'utf8'));
          eas.build.preview.env = {
            ...(eas.build.preview.env || {}),
            EXPO_PUBLIC_GIT_SHA: process.env.BUILD_GIT_SHA,
            EXPO_PUBLIC_BUILD_SOURCE: 'main'
          };
          fs.writeFileSync('eas.json', `${JSON.stringify(eas, null, 2)}\n`);
          console.log(`Stamped preview build with ${process.env.BUILD_GIT_SHA}`);
          NODE

      - name: Verify Expo authentication
        run: |
          if [ -z "${EXPO_TOKEN}" ]; then
            echo "EXPO_TOKEN repository secret is required."
            exit 1
          fi
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Verify EAS project link
        working-directory: apps/mobile
        run: |
          node -e "const app=require('./app.json'); if (!app.expo?.extra?.eas?.projectId) { console.error('Missing expo.extra.eas.projectId. Run eas init once and commit the generated project ID.'); process.exit(1); }"

      - name: Re-verify pinned delivery target
        run: |
          CHECKED_OUT="$(git rev-parse HEAD)"
          test "${CHECKED_OUT}" = "${GITHUB_SHA}" || {
            echo "Build target changed unexpectedly."
            exit 1
          }

      - name: Cancel older Expo preview builds
        working-directory: apps/mobile
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          CURRENT_SHA="$(git rev-parse HEAD)"
          : > /tmp/stale-eas-builds.txt

          for STATUS in new in-queue in-progress; do
            npx eas-cli@latest build:list \
              --platform android \
              --build-profile preview \
              --status "${STATUS}" \
              --limit 50 \
              --json > "/tmp/eas-${STATUS}.json"

            node - "/tmp/eas-${STATUS}.json" <<'NODE' >> /tmp/stale-eas-builds.txt
          const fs = require('fs');
          const path = process.argv[2];
          const data = JSON.parse(fs.readFileSync(path, 'utf8'));
          const builds = Array.isArray(data) ? data : [];
          for (const build of builds) {
            if (build?.id && build?.gitCommitHash) {
              console.log(`${build.id} ${build.gitCommitHash}`);
            }
          }
          NODE
          done

          sort -u /tmp/stale-eas-builds.txt -o /tmp/stale-eas-builds.txt

          while read -r BUILD_ID BUILD_SHA; do
            [ -n "${BUILD_ID}" ] || continue
            [ -n "${BUILD_SHA}" ] || continue
            [ "${BUILD_SHA}" != "${CURRENT_SHA}" ] || continue

            if git cat-file -e "${BUILD_SHA}^{commit}" 2>/dev/null && git merge-base --is-ancestor "${BUILD_SHA}" "${CURRENT_SHA}"; then
              echo "Cancelling older Expo build ${BUILD_ID} from ${BUILD_SHA}; delivery target is ${CURRENT_SHA}."
              npx eas-cli@latest build:cancel "${BUILD_ID}" --non-interactive
            else
              echo "Leaving Expo build ${BUILD_ID} (${BUILD_SHA}) alone because it is not a confirmed older ancestor of this delivery target."
            fi
          done < /tmp/stale-eas-builds.txt

      - name: Build installable Android preview
        working-directory: apps/mobile
        run: npx eas-cli@latest build --platform android --profile preview --non-interactive --clear-cache
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
