import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function MenuScreen() {
  return <PlaceholderScreen title="Menu" description="Profile, household, tickets, settings, support, and role-gated host tools will live here." />;
}
