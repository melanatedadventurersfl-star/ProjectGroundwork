import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function PassportScreen() {
  return <PlaceholderScreen title="Passport" description="Experience history, stamps, milestones, reflections, and Journey records will live here." />;
}
